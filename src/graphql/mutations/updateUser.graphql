mutation UpdateUser(
  $id: Int!
  $isBanned: Boolean
  $isSubmitter: Boolean
  $isVerified: Boolean
) {
  updateUser(
    id: $id
    isBanned: $isBanned
    isSubmitter: $isSubmitter
    isVerified: $isVerified
  )
}
